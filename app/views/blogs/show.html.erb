<h3><%= @blog.title %></h3>
<p><%= @blog.body %></p>

<%= link_to 'Edit', edit_blog_path(@blog) %>
<%= link_to 'Delete', blog_path(@blog), method: 'delete', data: {confirm: "Really Delete Blog Post?"} %>
<%= link_to 'Index', blogs_path %>